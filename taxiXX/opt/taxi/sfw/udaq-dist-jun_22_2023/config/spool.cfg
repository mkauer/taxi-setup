// spool.cfg
// defines a run data spool used by field_hub
//

// configuration of the generic "spool app", used by field hub to
// store runs on the local filesystem
spool_app = {
   spool_dir = "/home/root/data/udaq"
}
