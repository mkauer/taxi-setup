// default ext temperature sampling control settings
// imported by @include "./default_ext_temp_ctrl.cfg", replace include
// if per-channel variations are required

ext_temp_ctrl =
{
    moni_period_sec = 60;     // ext temp monitoring records perod 
                              // set to 0 to disable
    wild_ext_thresh = 160;    // threshold for discarding EXT TEMP readings (in 1/16 K)
                              // set to 0 to disable
}